#include "../includes/cub3d.h"

int wall_checker(char *line)
{
    
}

int check_map(t_data *data)
{
    t_map *map;
    int player;
    int i;

    map = data->file->map; 
    player = 0;
    i = 0;
    while(map->map[i] != NULL)
    {
        
        i++;
    }

}